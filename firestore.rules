rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow anyone to read the homepage content
      allow read: if true;
      // Allow only logged-in administrators to update content
      allow write: if request.auth != null;
    }
  }
}
